---
- name: Play to test connectivity
  hosts: workstation

  tasks:
    - debug:
        msg: "{{ lookup('env', 'OS_CLOUD')}}"

    - name: Check connectivity
      os_auth:
      register: os_auth_output

    - debug: var=os_auth_output
